<odoo>
    <!-- Action menu entry for list/form of purchase.order -->
    <record id="server_action_duplicate_with_confirm_purchase" model="ir.actions.server">
        <field name="name">📋New Duplicate</field>
        <field name="model_id" ref="purchase.model_purchase_order"/>
        <field name="binding_model_id" ref="purchase.model_purchase_order"/>
        <field name="binding_type">action</field>
        <field name="state">code</field>
        <field name="code">
action = env['ir.actions.act_window']._for_xml_id('imc_duplicate_multiple_models.action_open_duplicate_confirm_wizard')
        </field>
    </record>

</odoo>